export enum EUserStatus {
  ACTIVE = 'active',
  PENDING = 'pending',
  PENDING_ONBOARDING = 'pending_onboarding',
  BLOCKED = 'blocked',
}

export enum EUserRole {
  ADMIN = 'admin',
  REGULAR = 'regular',
}
